[build]
  command = "node build.js"
  publish = "/"

[build.environment]
  SECRETS_SCAN_OMIT_PATHS = "config.js,bundle.js"
  SECRETS_SCAN_OMIT_KEYS = "APP_USERNAME,APP_PASSWORD"
  SECRETS_SCAN_SMART_DETECTION_ENABLED = "false"
